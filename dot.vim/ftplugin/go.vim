" Filetype settings for go

" 挿入モードで <Tab> を入力した場合にスペースに置き換えしない（vim-goでも定義されている）
setlocal noexpandtab

" autoindent で挿入されるスペースの数。
setlocal shiftwidth=4

" 既に入力されている <Tab> をスペース何文字で表示するか。
setlocal tabstop=4
