" Filetype settings for ruby

" 挿入モードで <Tab> を挿入するとき、代わりに適切な数の空白を使う。
set expandtab

" 新しい行を開始したとき、新しい行のインデントを現在行と同じくする。
set autoindent

" ローカルファイル内の <Tab> が対応する空白の数。
set tabstop=2

" ローカル編集で <Tab> の幅として使用する空白の数。
set softtabstop=2

" ローカル(自動)インデントの各段階に使われる空白の数。
set shiftwidth=2
